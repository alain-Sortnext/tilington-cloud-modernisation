# ============================================================
# Tilington plc — Prometheus Configuration
# STATUS: STARTER — Candidate extends in Phase 6
# ============================================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: production
    programme: tilington-cloud-modernisation
    region: eu-west-2

# Alerting configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            # TODO: Candidate configures Alertmanager endpoint
            # - alertmanager:9093

# Load rules
rule_files:
  - "alerts/*.yml"
  # TODO: Candidate creates alert rules files

scrape_configs:
  # ── Kubernetes API Server ──
  - job_name: kubernetes-apiservers
    kubernetes_sd_configs:
      - role: endpoints
    scheme: https
    tls_config:
      ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
    bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
    relabel_configs:
      - source_labels: [__meta_kubernetes_namespace, __meta_kubernetes_service_name, __meta_kubernetes_endpoint_port_name]
        action: keep
        regex: default;kubernetes;https

  # ── Kubernetes Nodes ──
  - job_name: kubernetes-nodes
    kubernetes_sd_configs:
      - role: node
    scheme: https
    tls_config:
      ca_file: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
    bearer_token_file: /var/run/secrets/kubernetes.io/serviceaccount/token
    relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)

  # ── Tilington Microservices (auto-discovery) ──
  - job_name: tilington-microservices
    kubernetes_sd_configs:
      - role: pod
        namespaces:
          names:
            - tilington-prod
    relabel_configs:
      # Only scrape pods with prometheus.io/scrape: "true" annotation
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: "true"
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
        action: replace
        target_label: __metrics_path__
        regex: (.+)
      - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
        action: replace
        regex: ([^:]+)(?::\d+)?;(\d+)
        replacement: $1:$2
        target_label: __address__
      - action: labelmap
        regex: __meta_kubernetes_pod_label_(.+)
      - source_labels: [__meta_kubernetes_namespace]
        action: replace
        target_label: kubernetes_namespace
      - source_labels: [__meta_kubernetes_pod_name]
        action: replace
        target_label: kubernetes_pod_name

  # TODO: Candidate adds:
  # - Aurora PostgreSQL exporter scrape config
  # - AWS CloudWatch Exporter for AWS-native metrics
  # - Blackbox Exporter for external endpoint probing
  # - kube-state-metrics
